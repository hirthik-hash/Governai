# Frontend

This folder is intended for a Next.js + TypeScript + Tailwind app.
To create the app locally run:

```
npx create-next-app@latest frontend --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
```
